const state={
    value=0
};

const getter = {}
const mutations = {}
const actions = {}
export default {
    state,
    getter,
    mutations,
    actions 
// all these are key value pairs >> where key = value
}