// to get some common storage code to use all over the application
// >> we can write this code directly into the store.js too
//  make sure each file as its own unique namespace


export const updatedValue = (context,payload)=>{
   // context.commit('updatevalue',payload)
}